[project]
name = "tp-utils"
version = "0.0.5"
description = ""
authors = [
    {name = "Chris",email = "chris@topiaco.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[tool.poetry]
packages = [{include = "tp_utils", from = "src"}]

[tool.poetry.scripts]
test = "tp_utils.scripts:run_tests"
build = "tp_utils.scripts:build_package"
publish = "tp_utils.scripts:publish_package"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
